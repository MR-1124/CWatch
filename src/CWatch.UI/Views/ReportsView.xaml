<UserControl x:Class="CWatch.UI.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="980">
    <Grid Margin="28,20,28,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Row -->
        <Grid Grid.Row="0" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Storage Intelligence Report" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" FontFamily="Segoe UI Variable Display, Segoe UI, sans-serif"/>
                <TextBlock Text="Executive filesystem diagnosis answering &quot;Why Is My C: Drive Full?&quot;" FontSize="11.5" Foreground="{DynamicResource TextSecondary}" Margin="0,3,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="🔄 GENERATE REPORT" Command="{Binding GenerateReportCommand}" Style="{DynamicResource SecondaryButton}" Margin="0,0,6,0" Padding="12,6" FontSize="11"/>
                <Button Content="📋 COPY MARKDOWN" Command="{Binding CopyReportCommand}" Style="{DynamicResource SecondaryButton}" Margin="0,0,6,0" Padding="12,6" FontSize="11"/>
                <Button Content="⚡ EXPORT HTML" Command="{Binding ExportHtmlReportCommand}" Style="{DynamicResource ModernButton}" Padding="14,6" FontSize="11"/>
            </StackPanel>
        </Grid>

        <!-- Health Scorecard Banner -->
        <Border Grid.Row="1" Style="{DynamicResource HeroCardBorder}" Margin="0,0,0,12" Padding="14,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{DynamicResource BgCardNested}" BorderBrush="{DynamicResource BorderSubtle}" BorderThickness="1" CornerRadius="4" Padding="12,6" Margin="0,0,14,0" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="HEALTH SCORE: " FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextMuted}" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding HealthScore, StringFormat='{}{0}/100'}" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource AccentGreen}" VerticalAlignment="Center" Margin="4,0,0,0"/>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="{Binding HealthRating, StringFormat='SYSTEM STORAGE STATUS: {0}'}" FontSize="11.5" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>
                    <TextBlock Text="{Binding StatusMessage}" FontSize="11" Foreground="{DynamicResource TextSecondary}" Margin="0,2,0,0"/>
                </StackPanel>

                <!-- Key Takeaway Pill -->
                <Border Grid.Column="2" Background="{DynamicResource BgCardNested}" BorderBrush="{DynamicResource BorderSubtle}" BorderThickness="1" CornerRadius="3" Padding="8,4" VerticalAlignment="Center">
                    <TextBlock Text="AUDIT: PASS" FontSize="9.5" FontWeight="Bold" Foreground="{DynamicResource AccentGreen}"/>
                </Border>
            </Grid>
        </Border>

        <!-- Key Metrics Cards -->
        <Grid Grid.Row="2" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Total Capacity -->
            <Border Grid.Column="0" Style="{DynamicResource CardBorder}" Margin="0,0,6,0" Padding="14,10">
                <StackPanel>
                    <TextBlock Text="TOTAL CAPACITY" FontSize="9" FontWeight="Bold" Foreground="{DynamicResource TextMuted}"/>
                    <TextBlock Text="{Binding CurrentReport.DriveStatus.TotalBytes, Converter={StaticResource SizeFormatter}}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Used Space -->
            <Border Grid.Column="1" Style="{DynamicResource CardBorder}" Margin="3,0,3,0" Padding="14,10">
                <StackPanel>
                    <TextBlock Text="USED ALLOCATION" FontSize="9" FontWeight="Bold" Foreground="{DynamicResource TextMuted}"/>
                    <TextBlock Text="{Binding CurrentReport.DriveStatus.UsedBytes, Converter={StaticResource SizeFormatter}}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource AccentOrange}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Free Space -->
            <Border Grid.Column="2" Style="{DynamicResource CardBorder}" Margin="3,0,3,0" Padding="14,10">
                <StackPanel>
                    <TextBlock Text="AVAILABLE SPACE" FontSize="9" FontWeight="Bold" Foreground="{DynamicResource TextMuted}"/>
                    <TextBlock Text="{Binding CurrentReport.DriveStatus.FreeBytes, Converter={StaticResource SizeFormatter}}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource AccentGreen}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Reclaimable -->
            <Border Grid.Column="3" Style="{DynamicResource CardBorder}" Margin="6,0,0,0" Padding="14,10" BorderBrush="{DynamicResource AccentGreen}">
                <StackPanel>
                    <TextBlock Text="SAFELY RECOVERABLE" FontSize="9" FontWeight="Bold" Foreground="{DynamicResource AccentGreen}"/>
                    <TextBlock Text="{Binding CurrentReport.FormattedRecommendedCleanup}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource AccentGreen}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Markdown Report Body -->
        <Border Grid.Row="3" Style="{DynamicResource CardBorder}" Padding="16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox Text="{Binding ReportText, Mode=OneWay}" IsReadOnly="True" Background="Transparent" Foreground="{DynamicResource TextPrimary}" BorderThickness="0" FontFamily="Consolas, Cascadia Code, Courier New, monospace" FontSize="11.5" TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
