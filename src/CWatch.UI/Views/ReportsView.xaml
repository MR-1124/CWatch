<UserControl x:Class="CWatch.UI.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="980">
    <Grid Margin="32,24,32,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Row -->
        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Storage Intelligence Report" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" FontFamily="Segoe UI Variable Display, Aptos Display, Segoe UI, sans-serif"/>
                <TextBlock Text="Executive filesystem diagnosis answering &quot;Why Is My C: Drive Full?&quot;" FontSize="12.5" Foreground="{StaticResource TextSecondary}" Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="🔄 GENERATE REPORT" Command="{Binding GenerateReportCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Padding="12,6" FontSize="11"/>
                <Button Content="📋 COPY MARKDOWN" Command="{Binding CopyReportCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Padding="12,6" FontSize="11"/>
                <Button Content="🌐 EXPORT HTML" Command="{Binding ExportHtmlReportCommand}" Style="{StaticResource ModernButton}" Padding="14,6" FontSize="11"/>
            </StackPanel>
        </Grid>

        <!-- Key Metrics Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Total Capacity -->
            <Border Grid.Column="0" Style="{StaticResource CardBorder}" Margin="0,0,6,0" Padding="14">
                <StackPanel>
                    <TextBlock Text="TOTAL CAPACITY" FontSize="9.5" FontWeight="Bold" Foreground="{StaticResource TextMuted}"/>
                    <TextBlock Text="{Binding CurrentReport.DriveStatus.TotalBytes, Converter={StaticResource SizeFormatter}}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Used Space -->
            <Border Grid.Column="1" Style="{StaticResource CardBorder}" Margin="3,0,3,0" Padding="14">
                <StackPanel>
                    <TextBlock Text="USED ALLOCATION" FontSize="9.5" FontWeight="Bold" Foreground="{StaticResource TextMuted}"/>
                    <TextBlock Text="{Binding CurrentReport.DriveStatus.UsedBytes, Converter={StaticResource SizeFormatter}}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource AccentOrange}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Free Space -->
            <Border Grid.Column="2" Style="{StaticResource CardBorder}" Margin="3,0,3,0" Padding="14">
                <StackPanel>
                    <TextBlock Text="AVAILABLE SPACE" FontSize="9.5" FontWeight="Bold" Foreground="{StaticResource TextMuted}"/>
                    <TextBlock Text="{Binding CurrentReport.DriveStatus.FreeBytes, Converter={StaticResource SizeFormatter}}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource AccentGreen}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Reclaimable -->
            <Border Grid.Column="3" Style="{StaticResource CardBorder}" Margin="6,0,0,0" Padding="14" BorderBrush="{StaticResource AccentGreen}">
                <StackPanel>
                    <TextBlock Text="SAFELY RECOVERABLE" FontSize="9.5" FontWeight="Bold" Foreground="{StaticResource AccentGreen}"/>
                    <TextBlock Text="{Binding CurrentReport.FormattedRecommendedCleanup}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource AccentGreen}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Markdown Report Body -->
        <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="18">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox Text="{Binding ReportText, Mode=OneWay}" IsReadOnly="True" Background="Transparent" Foreground="{StaticResource TextPrimary}" BorderThickness="0" FontFamily="Consolas, Cascadia Code, Courier New, monospace" FontSize="12" TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
