<UserControl x:Class="CWatch.UI.Views.DuplicatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="980">
    <Grid Margin="28,20,28,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Row -->
        <Grid Grid.Row="0" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Duplicate Files Analyzer" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" FontFamily="Segoe UI Variable Display, Segoe UI, sans-serif"/>
                <TextBlock Text="Multi-phase SHA-256 byte hash detection to safely identify identical redundant files" FontSize="11.5" Foreground="{DynamicResource TextSecondary}" Margin="0,3,0,0"/>
            </StackPanel>

            <!-- Actions -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="⚡ SCAN DUPLICATES" Command="{Binding StartScanCommand}" Style="{DynamicResource ModernButton}" Margin="0,0,8,0" Padding="12,6" FontSize="11"/>
                <Button Content="🗑 MOVE TO RECYCLE BIN" Command="{Binding DeleteSelectedDuplicatesCommand}" Style="{DynamicResource DangerButton}" Padding="12,6" FontSize="11"/>
            </StackPanel>
        </Grid>

        <!-- Metrics & Smart Selection Banner -->
        <Border Grid.Row="1" Style="{DynamicResource HeroCardBorder}" Padding="14,10" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Left: Status & Reclaimable Metric -->
                <StackPanel VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,3">
                        <TextBlock Text="RECLAIMABLE DUPLICATE SPACE: " FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextMuted}" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding FormattedWastedBytes}" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource AccentOrange}" VerticalAlignment="Center" Margin="4,0,0,0"/>
                    </StackPanel>
                    <TextBlock Text="{Binding StatusMessage}" FontSize="11" Foreground="{DynamicResource TextSecondary}"/>
                </StackPanel>

                <!-- Right: Smart Selection Presets -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="KEEP NEWEST" Command="{Binding KeepNewestCopiesCommand}" Style="{DynamicResource SecondaryButton}" Margin="0,0,6,0" Padding="8,4" FontSize="10" ToolTip="Keeps the most recent copy in each group"/>
                    <Button Content="KEEP OLDEST" Command="{Binding KeepOldestCopiesCommand}" Style="{DynamicResource SecondaryButton}" Margin="0,0,6,0" Padding="8,4" FontSize="10" ToolTip="Keeps the oldest original file"/>
                    <Button Content="SELECT ALL" Command="{Binding SelectAllDuplicatesCommand}" Style="{DynamicResource SecondaryButton}" Margin="0,0,6,0" Padding="8,4" FontSize="10"/>
                    <Button Content="DESELECT ALL" Command="{Binding DeselectAllDuplicatesCommand}" Style="{DynamicResource SecondaryButton}" Padding="8,4" FontSize="10"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Duplicate Groups List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding DuplicateGroups}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{DynamicResource CardBorder}" Margin="0,0,0,10" Padding="14">
                            <StackPanel>
                                <!-- Group Header -->
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Border Background="{DynamicResource AccentOrange}" CornerRadius="2" Padding="6,2" Margin="0,0,8,0">
                                            <TextBlock Text="{Binding Files.Count, StringFormat='{}{0} IDENTICAL COPIES'}" FontSize="9" FontWeight="Bold" Foreground="White"/>
                                        </Border>
                                        <TextBlock Text="FILE WEIGHT: " Foreground="{DynamicResource TextMuted}" FontSize="10" FontWeight="Bold" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding FormattedSize, Mode=OneWay}" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" FontSize="11" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Grid>

                                <!-- File Entries in Group -->
                                <ItemsControl ItemsSource="{Binding Files}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="{DynamicResource BgCardNested}" CornerRadius="3" Padding="10,6" Margin="0,0,0,4" BorderBrush="{DynamicResource BorderSubtle}" BorderThickness="1">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <!-- Select Checkbox -->
                                                    <CheckBox Grid.Column="0" IsChecked="{Binding IsSelectedForDeletion}" Margin="0,0,10,0" ToolTip="Select for deletion"/>

                                                    <!-- File Path -->
                                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding FullPath}" Foreground="{DynamicResource TextPrimary}" FontSize="11" FontFamily="Consolas, Cascadia Code, monospace" TextTrimming="CharacterEllipsis"/>
                                                    </StackPanel>

                                                    <!-- Modified Date -->
                                                    <TextBlock Grid.Column="2" Text="{Binding LastModifiedUtc, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Foreground="{DynamicResource TextMuted}" FontSize="10.5" Margin="14,0" VerticalAlignment="Center"/>

                                                    <!-- Actions -->
                                                    <Button Grid.Column="3" Content="OPEN FOLDER" Command="{Binding DataContext.ShowInExplorerCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{DynamicResource SecondaryButton}" Padding="8,3" FontSize="9.5"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
