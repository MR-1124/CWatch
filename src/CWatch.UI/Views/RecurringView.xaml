<UserControl x:Class="CWatch.UI.Views.RecurringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="980">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="28,20,28,32">
            <!-- Header Row -->
            <Grid Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Recurring Growth Detector" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" FontFamily="Segoe UI Variable Display, Segoe UI, sans-serif"/>
                    <TextBlock Text="What Keeps Coming Back? Detect regenerating caches, runaway build folders &amp; background bloat" FontSize="11.5" Foreground="{DynamicResource TextSecondary}" Margin="0,3,0,0"/>
                </StackPanel>

                <Button Grid.Column="1" Content="🔄 RE-ANALYZE" Command="{Binding RefreshCommand}" Style="{DynamicResource SecondaryButton}" VerticalAlignment="Center" Padding="12,6" FontSize="11"/>
            </Grid>

            <!-- Overview Intelligence Banner -->
            <Border Style="{DynamicResource HeroCardBorder}" Padding="14" Margin="0,0,0,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Width="6" Height="6" CornerRadius="3" Background="{DynamicResource AccentCyan}" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="Recurring growth occurs when build engines, package managers, or background services re-accumulate large caches after you clean them. C:Watch monitors these cycles to pinpoint root causes." Foreground="{DynamicResource TextSecondary}" FontSize="11.5" TextWrapping="Wrap" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Status message -->
            <TextBlock Text="{Binding StatusMessage}" FontSize="11" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,12"/>

            <!-- Empty State when no alerts -->
            <Border Background="Transparent" Visibility="{Binding Alerts.Count, Converter={StaticResource InverseBoolToVisibility}}" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="30">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="No abnormal recurring cache regeneration detected." FontSize="13" FontWeight="Bold" Foreground="{DynamicResource AccentGreen}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Your developer environments, package managers, and system temp folders are within normal operating bounds." FontSize="11.5" Foreground="{DynamicResource TextMuted}" Margin="0,4,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Alerts List -->
            <ItemsControl ItemsSource="{Binding Alerts}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{DynamicResource CardBorder}" Margin="0,0,0,10" Padding="14">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Top Row: Path & Growth Rate Badge -->
                                <Grid Grid.Row="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Border Background="{DynamicResource BgCardNested}" BorderBrush="{DynamicResource AccentAmber}" BorderThickness="1" CornerRadius="3" Padding="6,2" Margin="0,0,8,0">
                                            <TextBlock Text="RECURRING ACCUMULATION" FontSize="9" FontWeight="Bold" Foreground="{DynamicResource AccentAmber}"/>
                                        </Border>
                                        <TextBlock Text="{Binding DisplayPath}" FontWeight="Bold" FontSize="12.5" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <Border Grid.Column="1" Background="{DynamicResource BgCardNested}" CornerRadius="3" Padding="8,3" BorderBrush="{DynamicResource BorderSubtle}" BorderThickness="1">
                                        <TextBlock Text="{Binding FormattedDailyRate, StringFormat='REGROWTH ~{0}'}" FontWeight="Bold" Foreground="{DynamicResource AccentAmber}" FontSize="10.5"/>
                                    </Border>
                                </Grid>

                                <!-- Middle Row: Explanation & Metrics -->
                                <StackPanel Grid.Row="1" Margin="0,10,0,12">
                                    <TextBlock Text="{Binding Explanation}" Foreground="{DynamicResource TextSecondary}" FontSize="11.5" TextWrapping="Wrap"/>
                                    
                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <TextBlock Text="CURRENT WEIGHT: " Foreground="{DynamicResource TextMuted}" FontSize="10" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding FormattedCurrentSize, Mode=OneWay}" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" FontSize="10.5" Margin="0,0,16,0"/>
                                        
                                        <TextBlock Text="REGROWN DELTA: " Foreground="{DynamicResource TextMuted}" FontSize="10" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding FormattedRegrown, StringFormat='{}{0} re-accumulated'}" FontWeight="Bold" Foreground="{DynamicResource AccentAmber}" FontSize="10.5"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Bottom Row: Actions -->
                                <StackPanel Grid.Row="2" Orientation="Horizontal">
                                    <Button Content="INSPECT DIRECTORY" Command="{Binding DataContext.InspectItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{DynamicResource SecondaryButton}" Padding="10,4" FontSize="10" Margin="0,0,6,0"/>
                                    <Button Content="📋 COPY MITIGATION COMMAND" Command="{Binding DataContext.CopyMitigationCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{DynamicResource SecondaryButton}" Padding="10,4" FontSize="10" Margin="0,0,6,0"/>
                                    <Button Content="DISMISS" Command="{Binding DataContext.IgnoreItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Background="Transparent" Foreground="{DynamicResource TextMuted}" BorderThickness="0" Cursor="Hand" Padding="8,4" FontSize="10" FontWeight="Bold"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
