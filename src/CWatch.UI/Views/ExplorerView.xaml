<UserControl x:Class="CWatch.UI.Views.ExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="980">
    <Grid Margin="32,24,32,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Row -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="260"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Storage Explorer" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" FontFamily="Segoe UI Variable Display, Aptos Display, Segoe UI, sans-serif"/>
                <TextBlock Text="Hierarchical breakdown of directories, package stores, build caches, and system allocations" FontSize="12.5" Foreground="{StaticResource TextSecondary}" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Search Filter Box -->
            <TextBox Grid.Column="1" Text="{Binding SearchFilter, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" ToolTip="Search files and folders in current directory"/>
        </Grid>

        <!-- Breadcrumb & Navigation Bar -->
        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Padding="10,6" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Back / Up Button -->
                <Button Grid.Column="0" Command="{Binding NavigateUpCommand}" Style="{StaticResource SecondaryButton}" Padding="10,5" Margin="0,0,10,0" ToolTip="Navigate up to parent directory">
                    <TextBlock Text="⬆ PARENT" FontSize="10.5" FontWeight="Bold"/>
                </Button>

                <!-- Breadcrumbs -->
                <ScrollViewer Grid.Column="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" VerticalAlignment="Center">
                    <ItemsControl ItemsSource="{Binding Breadcrumbs}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Content="{Binding Name}" Command="{Binding DataContext.NavigateToItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Background="Transparent" Foreground="{StaticResource AccentOrange}" BorderThickness="0" Cursor="Hand" FontWeight="Bold" FontSize="12.5" Padding="4,2"/>
                                    <TextBlock Text="/" Foreground="{StaticResource TextMuted}" Margin="3,0" VerticalAlignment="Center" FontSize="13" FontWeight="Bold"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Item Count Label -->
                <Border Grid.Column="2" Background="{StaticResource BgCardSecondary}" CornerRadius="3" Padding="8,3" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1">
                    <TextBlock Text="{Binding DisplayedItems.Count, StringFormat='{}{0} ITEMS'}" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextMuted}" VerticalAlignment="Center"/>
                </Border>
            </Grid>
        </Border>

        <!-- Items Table -->
        <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="0">
            <ListView ItemsSource="{Binding DisplayedItems}" Background="Transparent" BorderThickness="0" SelectionMode="Single">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem" BasedOn="{StaticResource {x:Type ListViewItem}}">
                        <EventSetter Event="MouseDoubleClick" Handler="OnItemDoubleClick"/>
                        <Setter Property="ContextMenu">
                            <Setter.Value>
                                <ContextMenu Background="{StaticResource BgCard}" BorderBrush="{StaticResource BorderLight}">
                                    <MenuItem Header="📂 Open in File Explorer" Command="{Binding DataContext.ShowInExplorerCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Foreground="{StaticResource TextPrimary}"/>
                                    <MenuItem Header="📋 Copy Full Path" Command="{Binding DataContext.CopyPathCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Foreground="{StaticResource TextPrimary}"/>
                                    <MenuItem Header="ℹ Show File Properties" Command="{Binding DataContext.ShowPropertiesCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Foreground="{StaticResource TextPrimary}"/>
                                </ContextMenu>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <!-- Name & Icon -->
                        <GridViewColumn Header="NAME" Width="400">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding IsDirectory, Converter={StaticResource CategoryColorConverter}, ConverterParameter='icon'}" Margin="0,0,8,0" FontSize="13" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" FontSize="12.5"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- Formatted Size -->
                        <GridViewColumn Header="ALLOCATED SIZE" Width="140">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding DisplaySize}" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" FontSize="12.5"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- Item Count (Folders) -->
                        <GridViewColumn Header="CONTAINS" Width="110">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding FileCount, StringFormat='{}{0:N0} files'}" Foreground="{StaticResource TextMuted}" VerticalAlignment="Center" FontSize="11"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- Last Modified Date -->
                        <GridViewColumn Header="LAST MODIFIED" Width="170">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding LastModifiedUtc, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Foreground="{StaticResource TextMuted}" VerticalAlignment="Center" FontSize="11"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </Grid>
</UserControl>
