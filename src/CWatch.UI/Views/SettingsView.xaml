<UserControl x:Class="CWatch.UI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="980">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="32,24,32,36">
            <!-- Header Row -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="System Configuration &amp; Preferences" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" FontFamily="Segoe UI Variable Display, Aptos Display, Segoe UI, sans-serif"/>
                    <TextBlock Text="Configure background monitoring intervals, growth thresholds &amp; automated parameters" FontSize="12.5" Foreground="{StaticResource TextSecondary}" Margin="0,4,0,0"/>
                </StackPanel>

                <!-- Actions -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="RESTORE DEFAULTS" Command="{Binding ResetDefaultsCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Padding="12,7" FontSize="10.5"/>
                    <Button Content="💾 SAVE CONFIGURATION" Command="{Binding SaveSettingsCommand}" Style="{StaticResource ModernButton}" Padding="14,7" FontSize="11"/>
                </StackPanel>
            </Grid>

            <!-- Status banner -->
            <TextBlock Text="{Binding StatusMessage}" FontSize="12" Foreground="{StaticResource AccentGreen}" FontWeight="Bold" Margin="0,0,0,14"/>

            <!-- Privacy & Security Guarantee Banner -->
            <Border Style="{StaticResource HeroCardBorder}" Padding="16" Margin="0,0,0,18" BorderBrush="{StaticResource AccentGreen}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="🛡️" FontSize="22" Margin="0,0,14,0" VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="100% OFFLINE &amp; ZERO NETWORK TELEMETRY" FontWeight="Bold" FontSize="12" Foreground="{StaticResource AccentGreen}"/>
                        <TextBlock Text="C:Watch executes strictly on your local device. No file paths, hashes, or capacity metrics are transmitted across the network." Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Group 1: Background Monitoring -->
            <Border Style="{StaticResource CardBorder}" Margin="0,0,0,18">
                <StackPanel>
                    <TextBlock Text="BACKGROUND MONITORING &amp; DISK ALARMS" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,16"/>

                    <!-- Enable Background Monitoring Toggle -->
                    <CheckBox IsChecked="{Binding Settings.MonitoringEnabled}" Margin="0,0,0,16">
                        <StackPanel Margin="8,0,0,0">
                            <TextBlock Text="Enable Background Storage Monitor" FontWeight="Bold" FontSize="13"/>
                            <TextBlock Text="Periodically records lightweight differential snapshots to detect silent growth and calculate burn rate." Foreground="{StaticResource TextMuted}" FontSize="11.5" Margin="0,2,0,0"/>
                        </StackPanel>
                    </CheckBox>

                    <!-- Interval Setting -->
                    <Grid Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="130"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Sampling Interval (Minutes)" FontWeight="SemiBold" FontSize="13"/>
                            <TextBlock Text="How frequently C:Watch inspects drive capacity (Default: 30m)" Foreground="{StaticResource TextMuted}" FontSize="11.5"/>
                        </StackPanel>
                        <TextBox Grid.Column="1" Text="{Binding Settings.MonitorIntervalMinutes}" VerticalAlignment="Center"/>
                    </Grid>

                    <!-- Low Disk Warning Threshold -->
                    <Grid Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="130"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Critical Low Space Threshold (GB)" FontWeight="SemiBold" FontSize="13"/>
                            <TextBlock Text="Trigger critical warning when free space drops below this level (Default: 10 GB)" Foreground="{StaticResource TextMuted}" FontSize="11.5"/>
                        </StackPanel>
                        <TextBox Grid.Column="1" Text="{Binding Settings.LowDiskWarningThresholdGB}" VerticalAlignment="Center"/>
                    </Grid>

                    <!-- Daily Growth Alert Threshold -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="130"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Rapid Growth Trigger (GB / 24h)" FontWeight="SemiBold" FontSize="13"/>
                            <TextBlock Text="Alert when daily consumption exceeds this rate (Default: 5 GB/day)" Foreground="{StaticResource TextMuted}" FontSize="11.5"/>
                        </StackPanel>
                        <TextBox Grid.Column="1" Text="{Binding Settings.GrowthAlertThresholdGBPerDay}" VerticalAlignment="Center"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Group 2: Scanning & Automation -->
            <Border Style="{StaticResource CardBorder}" Margin="0,0,0,18">
                <StackPanel>
                    <TextBlock Text="INDEXING &amp; SCANNER PARAMETERS" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,16"/>

                    <!-- Auto Scan on Launch -->
                    <CheckBox IsChecked="{Binding Settings.AutoScanOnLaunch}" Margin="0,0,0,16">
                        <StackPanel Margin="8,0,0,0">
                            <TextBlock Text="Run Automatic Storage Scan on Startup" FontWeight="Bold" FontSize="13"/>
                            <TextBlock Text="Initiates a fast asynchronous scan immediately when C:Watch opens." Foreground="{StaticResource TextMuted}" FontSize="11.5" Margin="0,2,0,0"/>
                        </StackPanel>
                    </CheckBox>

                    <!-- Top Largest Files Limit -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="130"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Top Largest Files Locator Index Limit" FontWeight="SemiBold" FontSize="13"/>
                            <TextBlock Text="Number of top files tracked in memory (Default: 100)" Foreground="{StaticResource TextMuted}" FontSize="11.5"/>
                        </StackPanel>
                        <TextBox Grid.Column="1" Text="{Binding Settings.TopLargestFilesLimit}" VerticalAlignment="Center"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Group 3: Snapshot Retention -->
            <Border Style="{StaticResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="SQLITE ARCHIVE RETENTION" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,16"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="130"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Snapshot History Retention (Days)" FontWeight="SemiBold" FontSize="13"/>
                            <TextBlock Text="Automatically purges historical snapshots older than this limit to maintain a compact database." Foreground="{StaticResource TextMuted}" FontSize="11.5"/>
                        </StackPanel>
                        <TextBox Grid.Column="1" Text="{Binding Settings.SnapshotRetentionDays}" VerticalAlignment="Center"/>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
